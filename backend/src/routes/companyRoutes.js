const express = require('express');
const router = express.Router();
// ...company controller imports...

// router.post('/register', ...);
// router.get('/profile', ...);
// router.put('/profile', ...);
// router.post('/upload-logo', ...);
// router.post('/upload-banner', ...);

module.exports = router;
